_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.DS_Store
JNSKM_Multi-Agent_Website_Reviewer.*

# Music automation tracking file (video IDs already processed)
.music-tracker.json
# Note: .streaming-links-retry.json is NOT ignored - it needs to persist between CI runs

# Secrets — never commit. Use .env.local for API keys.
.env.local
.env.*.local

# Netlify build artifacts
.netlify/
netlify/functions/node_modules/
netlify/functions/package-lock.json
